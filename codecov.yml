# Keep `target` values in sync with Makefile COVERAGE_THRESHOLD.
coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 80%
        threshold: 5%
